podLabels:
  # kubernetes-mixin node rules select job="node-exporter"; alloy-telemetry's
  # targets_node_exporter maps target job from this pod label (cmdshift/platform#146)
  jobLabel: node-exporter
resources:
  requests:
    cpu: 10m
    memory: 32Mi
  limits:
    cpu: 500m
    memory: 48Mi
