apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: security
  namespace: flux-system
spec:
  interval: 1h
  retryInterval: 5s
  dependsOn:
    - name: namespaces
    - name: sources
    - name: crds
    - name: storage-config
  sourceRef:
    kind: Bucket
    name: main
  path: ./manifests/bases/security
  prune: true
  wait: true
  timeout: 10m
