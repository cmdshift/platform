apiVersion: seaweed.seaweedfs.com/v1
kind: S3Credentials
metadata:
  name: loki
  namespace: objects
spec:
  seaweedRef:
    name: main
    namespace: objects
  identityRef:
    name: loki
  secretRef:
    name: loki-s3-credentials
