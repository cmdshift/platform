# Cloud cluster notes (self-managed Talos)

Things to do differently from the local Talos-in-Docker cluster when standing up a cloud cluster. The inventory of deliberately local-only settings lives in the `manifests/bases/<group>/README.md` files (grouped by workload; cross-cutting conventions + the hardening-deviations baseline in `manifests/README.md`).

## Storage: local-path reclaim policy + volume binding (from cmdshift/platform#12)

Both `reclaimPolicy` and `volumeBindingMode` are **StorageClass-level** settings — there is no per-PVC override in general use:

- `volumeBindingMode: WaitForFirstConsumer` — non-negotiable in a multi-node cloud cluster: volumes must bind after pod scheduling so they land on the node the pod runs on. (We already default this locally, so it carries over as-is.)
- `reclaimPolicy` — the PV snapshots the class's policy at provision time. Changing the class later only affects newly provisioned PVs; existing PVs keep `Delete` (or `Retain`) until patched individually.
  - Selective overrides: a separate `local-path-retain` class for data that can't be regenerated, or patch the PV's `persistentVolumeReclaimPolicy` directly. A PVC-level `spec.persistentVolumeReclaimPolicy` field exists upstream (KEP-3939, alpha in 1.32, feature-gated) — verify its status in our k8s version before relying on it.
  - Keep `Delete` as the class default in the cloud too: `Retain` everywhere trades accidental-deletion safety for a steady accumulation of `Released` PVs nobody cleans up. Protect the few volumes that matter, not all of them.

Bigger caveat first: local-path is node-local with no replication — a lost node is a lost volume. In the cloud it's only appropriate for rebuildable state (caches, scratch); durable data belongs on replicated storage or off-cluster S3 (the velero → rustfs pattern we already use locally). Decide the real storage story before spending time tuning local-path policy.

**Velero + local-path volume data — solved locally with a one-annotation fix** (drill 2026-09-05, runbooks/local/velero-backups.md): velero FSB skips hostPath PVs, and local-path-provisioner defaults to hostPath — but its `defaultVolumeType: local` StorageClass annotation makes it emit `local` PVs, which velero FSB backs up natively (no CSI plugin needed). The local StorageClasses carry the annotation; the cloud cluster needs the same on its StorageClasses if it keeps local-path. Velero's temporary data mover pods need resources (`node-agent-config` configmap) and a PolicyException scoped by the `velero.io/pod-volume-*` labels.

Velero's kopia repo-maintenance Jobs are admission-checked too (velero builds their pod spec internally with no securityContext/resources) — the local fix-first hardening in `velero-values.yaml` carries over as-is, with one image-derived caveat: `runAsUser: 1002` was read out of the v1.18.1 image's `/etc/passwd` (USER `cnb` is non-numeric, so kubelet can't verify bare `runAsNonRoot`; the image has no shell — use `docker create` + `docker export` to read it). Re-resolve the uid on any velero image bump. Retention carries over too: the `pvcs` schedule's 72h TTL keeps at most 3 backup generations live (cmdshift/platform#109).

Velero's data-mover hosting pods hardcode `TerminationGracePeriodSeconds: 0` in velero's source (v1.18.1 exposer builders — no values knob, no upstream issue), so wherever `require-graceful-termination` (or any tgps floor) is enforced, the velero PolicyException must also carry that policy's ref — the local one does (cmdshift/platform#111). The velero alerts carry over: run the `backup-alerts` group from `observability/mimir-rules.yaml` (partial-failure counter + `absent()` arm for the last-success gauge + repo-maintenance rule — see `manifests/bases/observability/README.md`).

**Volume expansion**: the local classes set `allowVolumeExpansion: false` because local-path-provisioner has no expansion code at all (a `true` value just wedges any PVC resize forever — evidence in `manifests/bases/storage/README.md`). Any real CSI StorageClass in the cloud (EBS, Ceph, …) expands natively — set `allowVolumeExpansion: true` there, and size local-path-hosted stateful workloads for the recreate-with-bigger-PVC path if any stay on local-path.

## Admission policy: graceful-termination floor (local: `require-graceful-termination`, cmdshift/platform#89)

The `terminationGracePeriodSeconds >= 5` ValidatingPolicy carries to the cloud unchanged. The PolicyExceptions for the 1s-terminating system agents (cilium, cilium-envoy, hubble-relay, tetragon) need a keep/drop decision: local keeps them because the docker containers are throwaway and fast node shutdown is the recovery path; cloud Talos nodes with real etcd/quorum roles should re-verify cilium's 1s is still right there (a 1s agent grace on a node draining real workloads is a different trade than on a 2-container test rig).

## Compute quotas + LimitRange (local: `<group>-config/resource-quota.yaml`, cmdshift/platform#93)

Every local workload namespace carries a `ResourceQuota/compute` sized from local-cluster audit numbers (2026-09 audit; evidence in each quota's rationale comment). Cloud deltas:

- **Re-size and re-scope for the cloud's namespace set** — the cloud namespace layout will differ (and may add CI/ephemeral namespaces); the local quota objects' hard values are local-cluster numbers and must be re-audited there, not carried over. Group/namespace layout reference: locally the observability stack collapsed into one `observability`/`observability-config` pair (cmdshift/platform#120 — metrics+monitoring+logging merged; operators install into a namespace named after their kustomization group) — mirror that collapsed layout in the cloud or document the deviation; if quotas from separate namespaces ever fold into one, rename them (two `ResourceQuota/compute` objects in one namespace would both charge every pod).
- **LimitRange is the defaults source in the cloud, and not dead code locally either** — locally kyverno Deny-mode admission denies containers missing requests/limits, **but kyverno PolicyExceptions don't skip ResourceQuota admission** (cmdshift/platform#111): a container exempted via PolicyException (no resources, no config knob) is still denied by the `compute` quota, and the only defaults source for it is a LimitRange. `observability` carries `LimitRange/compute-defaults` locally for exactly that case (introduced for the thanos-operator's config-reloader sidecar — the operator is gone since the LGTM migration, cmdshift/platform#128, but the LimitRange stays as the defaults source for any future exception-exempt container). In the cloud add a LimitRange per namespace so bare containers get defaults under whatever admission policy runs there. Quota-vs-exception mechanics: [runbooks/local/adding-a-workload.md](../../runbooks/local/adding-a-workload.md).

## Cilium (local: manifests/bases/networking/cilium.helm-release.yaml)

What must change vs the local helm release:

- **Chart version: run the latest stable 1.20.x, not the local pin** — the local tree pins `1.21.0-pre.2` only because the Linux test host's kernel 7.2 crashes 1.20.x agents at startup (FnSetRetval verifier rejection, cilium/cilium#48016). Cloud nodes on a normal kernel take the stable release. Note the local pin lives in two places (bootstrap `cluster/local/bootstrap/main.tf` + the flux HelmRelease) and must move in lockstep on any local bump.
- `kubeProxyReplacement: true` is **no longer a delta** — true everywhere since cmdshift/platform#70 (it's the cilium Gateway API controller prerequisite; locally the bootstrap-era `false` blocked the GatewayClass from ever claiming). In the cloud, pair it with the Talos side the same way: `proxy.disabled: true` in the machine config, then a one-time `kubectl -n kube-system delete ds kube-proxy` — Talos stops rendering kube-proxy but never deletes the already-applied DaemonSet. Ordering: KPR live first, kube-proxy removal second (the ClusterIP DNAT gap between the two makes coredns unreachable and flux deadlocks). A values-only helm upgrade does NOT restart cilium-operator — restart the deployment after the upgrade or the process keeps old startup flags.
- `k8sServiceHost: localhost` / `k8sServicePort: 7445` → the real control-plane endpoint (Talos cluster/API VIP or a proper LB). The local values point at the per-node docker haproxy (`cluster/local/nodes/main.tf`) and exist because cilium must reach the API before pod networking exists — a bootstrap chicken-and-egg that doesn't apply on real VMs.
- `cgroup.autoMount.enabled: false` + `hostRoot` → drop; that's a Talos-in-docker container quirk. Use chart defaults on real nodes.
- `gatewayAPI.hostNetwork: true` (+ `k8s-role/work` node match) → normal non-hostNetwork gateway API listeners fronted by a cloud LB. The local form exists to publish LB ports on the docker host.
- `l2announcements` → only if the cloud VMs share an L2 segment; otherwise replace with the LB story above.
- **Encryption: keep wireguard, ztunnel is not available on 1.20.x** (cmdshift/platform#87). Local runs `encryption.type: ztunnel` (the beta Istio-ambient L4 mTLS proxy, cilium/cilium#38548) — but the feature ships only in the 1.21-pre line the local pin carries; stable 1.20.x has no ztunnel, and the old CNP `authentication.mode` mutual-auth it would pair with is **deprecated in 1.20 and removed in 1.21** (cilium/cilium#47132 — no SPIRE mutual auth anywhere). Cloud decision: stay on wireguard until (a) cilium 1.21 (or later) is GA and validated, then re-evaluate ztunnel with its full CA story — cloud would want cert-manager/SPIRE-managed CA material with real rotation, not the local self-signed-10y shortcut; or (b) an app-level mTLS layer is adopted instead. Do NOT port the local ztunnel values (or the `postRenderers` remapping the cert-manager secrets onto `/etc/ztunnel` filenames — chart-shape-coupled) to the cloud release as-is; they depend on the local `certificates-config` ztunnel CA objects and the enrolled-namespace registry (`io.cilium/mtls-enabled=true`; no local namespace is enrolled by default).

Keep as-is (validate under real traffic): wireguard encryption, `ipam.mode: kubernetes`, resource sizing (re-audit — local sizing was tuned for idle test loads, not real traffic).

## Kubescape (removed locally 2026-09-06)
The kubescape operator and all its SecurityExceptions were removed from the manifests tree (too heavy for what it delivered; single-purpose hardening tools are the replacement — see the `manifests/bases/security/README.md`). Do **not** reintroduce it in the cloud: the hardening settings it drove (explicit non-root uid/gid, roFS, caps drop, SA-token off) stay in the manifests as NSA-guidance comments, and the accepted-deviations baseline lives in `manifests/README.md` for the replacement tools to audit against.
- Flux polling: the local cluster loosened the bucket to 5m and kustomization drift-heal to 1h for interactive determinism (single operator, frequent manual `sync_wait` + `flux_wait`). Keep the tighter 1m/10m in the cloud — with multiple operators, frequent drift-heal is the enforcement of everything-in-files.

## kube-bench CIS scan (episodic, first run locally 2026-09-08)

Carry over as-is: kube-bench as an episodic CLI-style one-shot Job (temp scaffolding pattern — privileged PSS namespace + scoped PolicyException, delete after; spec reference in `cluster/local/.tmp/kube-bench-scan/`, method + triage ledger in `manifests/bases/security/README.md`). Pin the benchmark explicitly — kube-bench releases lag k8s (cis-2.0 covers 1.34–1.35 but isn't in a released kube-bench yet; check `docs/platforms.md` per bump).

Cloud deltas — the scan gets **simpler and more truthful** on non-Talos-in-docker nodes: systemd unit paths, `/var/lib/etcd`, and full `/etc/kubernetes/pki` all exist, so the local false-FAIL map (authorization-config file form excepted — that's Talos defaulting, re-verify there) largely disappears; expect near-zero layout noise. Keep the local ledger's real findings on the radar: `--service-account-extend-token-expiration`, scheduler/controller-manager bind addresses, `--kubelet-certificate-authority`. (The 4.3.1 kube-proxy metrics finding dropped out with the kube-proxy removal — cilium KPR=true, cmdshift/platform#70.)

**Controller-manager/scheduler `bind-address: 0.0.0.0`** (set in `cluster/local/nodes/templates/cluster.tftpl.yaml` so prometheus can scrape 10257/10259 by node IP; protected by TLS + bearer token): on cloud VMs with public IPs, restrict 10257/10259 to the cluster's internal CIDR with a security-group/firewall rule.

## Tetragon (local: `manifests/bases/security/`, adopted 2026-09-07)

Chart 1.7.1 from the same `cilium` HelmRepository as Cilium — no extra source. Carries over as-is: the release values, the PSS-`privileged` `security` namespace, and the `allow-tetragon-security-contexts` PolicyException scoped to the namespace + `tetragon` prefix (the agent DaemonSet is privileged with host paths `/proc`, `/sys/fs/bpf`, `/sys/kernel/tracing` by design; same shapes exist for cilium/node-exporter today). The `mount-tracefs` init container the local release patches in via `postRenderers` is a **Talos-in-Docker container-mode workaround** (container-mode machined skips the pseudo-fs submounts, so `/sys/kernel/tracing` stays an unmounted sysfs dir) — real Talos mounts tracefs, and the patch's fstype guard (`stat -f -c %t` = `74726163` → no-op) makes it a no-op there: remove-in-the-cloud candidate, rationale in [manifests/bases/security/README.md](../bases/security/README.md). The `disable-kprobe-multi` agent arg is likewise host-kernel-specific (kernel 7.2 rejects kprobe_multi attach for Override actions on `security_kernel_module_request`) — drop it in the cloud unless the kernel there shows the same attach failure.

Cloud deltas:
- **gRPC exposure**: the local cluster binds the tetra gRPC listener to node loopback (`tetragon.grpc.address: localhost:54321`) so `kubectl port-forward` + `tetra --server-address localhost:54321` works with zero node-IP exposure. That's acceptable in the cloud too for admin access through the API server, but if anything needs remote gRPC, enable `tetragon.grpc.tls` (chart supports cert-manager/cronJob methods + client-cert enforcement) — never plain TCP on a node IP.
- **Enforcement TracingPolicies**: observability-first locally; if enforcement policies ship in the cloud, stage them per-namespace (`TracingPolicyNamespaced`) and watch for workload breakage before cluster-wide `TracingPolicy`. Stage-1 observe policies (`security-config/*.tracing-policy.yaml`, all `monitor_only`) carry over as-is; enforcement flips are manifest edits (`spec.options`), never `tetra tp set-mode` (flux is the source of truth). Local kernel findings to re-verify on real nodes: the linuxkit kernel lacks `bpf_lsm_*` BTF symbols so LSM-hook policies are unavailable there (kprobes on the same hooks work) — a cloud Talos kernel likely HAS working BPF-LSM (check `kallsyms | grep bpf_lsm_`), meaning `lsmhooks:` policies are possible there but the local kprobe-based policies still apply unchanged.
- **Stage-2 flip mechanics** (kernel-modules + exec deny-list flips live locally 2026-09-08, cmdshift/platform#28): the `policy-mode` option value is **`enforce`/`monitor`** — `enforcement`/`monitor_only` fail the agent with a load_error on every node; and `spec.options` is a **list of `{name, value}`**, not a map. Policies MUST carry `podSelector` or (with `enable-policy-filter: true`) their events never reach the export sink. The kernel-modules flip uses Override(EACCES) on benign module auto-load requests — every Go runtime probes missing modules on first socket(), so a strict Sigkill on the request hook would kill arbitrary controllers; keep that split in the cloud. **Talos denies `CAP_SYS_MODULE` to containers at runc create** (local finding) — re-verify on cloud nodes: if real module loads are reachable from pods there, the Sigkill path becomes live-fire and the 1-day kill-alert watch per flip is mandatory. The exec deny-list carries over with cloud-specific remapping: scoping is per-policy `podSelector` over pod labels because the 1.7.1 CRD has no `Kube` namespace matcher (upstream `matchWorkloads` with the `k8s:io.kubernetes.pod.namespace` expression is the cleaner shape once the chart/CRD is bumped), the label selectors must match the cloud cluster's actual release labels, and `security_bprm_check` presence must be re-verified in the node kallsyms. The `arg type: file` silent no-op on `security_bprm_check` is chart-version-independent — use `linux_binprm` and grep agent logs for `validation warning` after any new kprobe policy.
- **Log delivery**: tetragon's export-stdout events land in Loki via the alloy pipeline (`loki_query '{instance=~"security/tetragon.*"}'`) — the local ingestion outage (cmdshift/platform#27) is fixed; see the Logging section below for the carry-over landmine.
- **Kill-event visibility on 1.7.1** (cmdshift/platform#57, verified live locally 2026-09-09): `tetragon_kill_events_total` does not exist in any release — alert on `tetragon_policy_events_total{policy=~"kernel-modules|exec-deny-list-.*"}` instead (the local `observability/mimir-rules.yaml` carries the working rule; replicate it in the cloud's ruler). bprm_check enforcement events are **pod-less by design** (pre-exec attribution = the runc fork; namespace `""`), so the exporter denylist drops them from Loki — the event surfaces are `tetra getevents -o json` (compact renders them as `❓ syscall <node> runc security_bprm_check`) and the metric. The 1.8.0 bump (cmdshift/platform#59) upgrades the alert to `tetragon_tracingpolicy_selector_actions_total`.

## Logging (local: the `observability` group, alloy+loki adopted 2026-08-30)

Carries over as-is: the alloy DaemonSet + `config.alloy` pipeline (kustomize `alloy-config` ConfigMap → `alloy.configMap` wiring), the loki release (S3 to seaweed `main-s3.objects.svc:8333`, buckets `loki`/`loki-rules`, gateway default tenant `self-monitoring`), and the tenant preset in `loki_query`/`config.alloy`.

Cloud deltas:
- **`alloy.configMap` wiring is load-bearing** — the grafana/alloy chart **silently installs its example config** (pods healthy, nothing pushed, zero errors) if the values block (`create: false` / `name: alloy-config` / `key: config.alloy`) is missing; locally this was the #27 ingestion outage (dropped by an edit that overwrote the block, cmdshift/platform#27). Verify the running config from day one with `alloy_components`: the expected set is `discovery.kubernetes.pods` + `discovery.relabel.k8s_labels` + `loki.source.kubernetes.pods` + `loki.write.endpoint` — `discovery.kubernetes.{nodes,services,endpoints,...}` appearing means the example config is live. (2026-09-07: the intermediate `loki.process.wrap`/`stage.pack` was removed — it wrapped every line in `{"_entry": ...}` while carrying no metadata; keep lines app-native. River comments are `//`, never `#`.)
- **Stream labels**: `namespace`, `pod`, `container`, `node` are promoted by the alloy relabel pipeline (2026-09-07 — the label-pipeline follow-up this note used to wait for LANDED locally; same cardinality as `instance`), plus `instance` (`ns/pod:container`), `job`, `service_name`, `detected_level`. Select natively on the k8s labels; **every line in Loki is JSON** — the alloy pipeline normalizes (JSON pass-through, logfmt→JSON fields, plain-text→`{"msg": raw}`) and the components with knobs log JSON at the source (loki `-log.format=json`, kps `logFormat`s, mimir `server.log_format` in `mimir.config.yaml`, velero `configuration.logFormat`, grafana `GF_LOG_CONSOLE_FORMAT` env — the operator webhook rejects `spec.config.log.console`; kyverno `features.logging.format`; grafana-operator `logging.encoder`; metrics-server `--logging-format=json`; seaweedfs-operator via HelmRelease postRenderers — the sweep table is in `manifests/bases/observability/README.md`). Cilium/cert-manager/local-path/weed have no knob — the pipeline handles them.
- **Seaweed volume-server CPU sizing matters** (2026-09-07 local incident): the volume server CPU-throttled at 93% of CFS periods during vacuum, wedged with all volumes read-only, and seaweed S3 500'd every write for hours (loki couldn't flush chunks; master: `Not enough data nodes found!`). Limit is now 1000m locally — size the cloud volume server (and s3 gateway) with the same generous-CPU convention, and watch the loki bucket's PVC growth: `allowVolumeExpansion: false` makes retention/vacuum the only lever.
- **Loki delete-path tuning is local-only**: `retention_delete_delay: 5m` and `delete_request_cancel_period: 15m` shorten the compactor feedback loop for a test cluster (`# true in the cloud` comments at the values) — keep the upstream defaults (2h / 24h) in the cloud.

## API audit logging (local: `cluster/local/nodes/files/audit-policy.yaml` + alloy, cmdshift/platform#90)

The reviewed 9-rule audit policy file is the literal Kubernetes Policy body and moves **verbatim** into `KubeAuditPolicyConfig.configuration` once the cloud cluster runs Talos 1.14 (the config-CRD migration — cmdshift/platform#98); on 1.13-style inline config it stays under `cluster.apiServer.auditPolicy` (templated via `templatefile` + `indent`). `cluster.apiServer.auditLog` is not a 1.13 machine-config field — audit log rotation settings are hardcoded apiserver defaults in 1.13 (`--audit-log-maxage=30`, maxBackup 10, maxSize 100), so nothing to set there on either version until 1.14. The alloy scrape/mount/capability work carries as-is (ctrl toleration, `DAC_READ_SEARCH` on the 0700 audit dir); volume control lives in the policy's `none` rules for machine heartbeats — see the logging README. Local-only part: the `logging` namespace entry in `kubernetesTalosAPIAccess.allowedKubernetesNamespaces` is a Talos-in-Docker container-mode artifact (kubelet verifies image pulls through the Talos API there) — real Talos nodes likely don't need it, verify.

## SLO burn-rate alerting (local: `manifests/bases/observability/slo.rules.yaml`, cmdshift/platform#85)

The portable parts carry over as-is: the recording-rule/burn-alert template shape (header comment in `slo.rules.yaml` — naming `slo:<service>:<sli>:rate<window>` with **underscores**, not `-`; multiwindow multi-burn pairs; the `slo: "true"` routing label) and the child route in the AlertmanagerConfig matching that label — swap the receiver for the cloud's real one (mailpit is local-only delivery). Two cloud-relevant notes: the recording rules are evaluated by **mimir's ruler** (local backend — rule files are ConfigMap-mounted under `<dir>/<tenant>/`; cmdshift/platform#128), so the cloud needs equivalent ruler wiring; and if a ruler based on an operator that filters rule ConfigMaps is ever used there, check its ConfigMap intake rules first (the thanos-operator's one-data-key landmine is obsolete locally but the class of trap is not).

## PDBs: the auto-pdb GeneratingPolicy + the kube-system webhook exclusion (local: `policies-config/auto-pod-disruption-budget-multi-replica.generating-policy.yaml`, cmdshift/platform#84)

The `auto-pod-disruption-budget-multi-replica` GeneratingPolicy lands with the manifests — it generates a PodDisruptionBudget (maxUnavailable: 1) for every multi-replica Deployment/StatefulSet in flux-managed namespaces, so cloud databases (the reason the policy exists — cloudnative-pg et al. in `datastores/`) get PDBs without per-workload manifests. Two cloud-side verifications: (1) confirm databases actually get their PDB (check `kubectl get pdb -A` after deploying the first multi-replica database — the policy's generate path is event-driven on create/update, and `evaluation.generateExisting` covers pre-existing ones); (2) any multi-replica workload in **kube-system** needs an explicit PDB manifest, because the CEL policy's fine-grained webhook inherits `config.webhooks.namespaceSelector` from the kyverno values (which exclude kube-system) and **silently generates nothing there** — same on any cluster using that values shape. Escape hatch `pdb.kyverno.io/skip: "true"` for charts shipping their own PDB; split rationale in `manifests/bases/policies-config/README.md`.

## Traces: tempo (local: `manifests/bases/observability/`, adopted 2026-09, cmdshift/platform#83)

The tempo monolithic-mode release carries over as-is: chart values (incl. the top-level `persistence` key, `forcepathstyle` S3 config key, `-config.expand-env=true` extraArg), the ExternalSecret credentials pattern (secrets-server payload → `extraEnvFrom` → `${TEMPO_S3_*}`), the Bucket CR in `objects-config/`, and the ServiceMonitor on 9090. Tempo is currently **traceless** — the only instrumentation source was removed (below).

- **OTel collector is the sole scraper since cmdshift/platform#141 (local)**: kube-prometheus-stack (operator + Prometheus CR) is **removed**; the collector became `mode: statefulset` + `targetAllocator.prometheusCR` (ServiceMonitor discovery — the `prometheus-operator-crds` release stays CRDs-only so the SM CRDs exist) + a `prometheus` receiver static-scraping the control-plane (kubelet/cadvisor/apiserver/kcm/scheduler). The kubernetes-mixin essentials (Watchdog, KubeNodeNotReady, KubeDeploymentReplicasMismatch, KubeJobFailed) moved into the mimir ruler, and the Alertmanager is chart-managed (`prometheus-community/alertmanager`, raw config — snake_case route keys, string matchers). The full trap list (TA pod inherits nothing from the CR spec; TA SA needs scrapeconfigs/probes/secrets RBAC; kubelet SAR needs `nodes/metrics`; mimir needs `name_validation_scheme: "utf8"` + raised `ingestion_rate`; the prw exporter treats 429 as permanent) lives in [manifests/bases/observability/README.md](../bases/observability/README.md). Cloud adoption should mirror that whole section, not just the collector deltas below: use the `opentelemetry-collector-contrib` collector image (the chart's default `opentelemetry-collector-k8s` lacks the prometheusremotewrite exporter — `unknown type` at startup), and note the v1beta1 CRD takes `spec.config` as a structured object (v1alpha1 took a string). The collector's k8sattributes processor needs pods list/watch — a ClusterRole+ClusterRoleBinding is the shape that worked (`observability-config/otel-collector.rbac.yaml`; the namespace-scoped RoleBinding path failed to propagate in the local recreated-SA case, cmdshift/platform#128).

- **Beyla was removed locally (2026-09-18): its eBPF probes panic the host kernel (7.2).** Do not adopt beyla (or any new eBPF-probe workload) on a host without verifying kernel compatibility first — same constraint class as the cilium version pin. If the cloud ever re-adopts it, it also needs the privileged PSS + a `allow-beyla-ebpf`-shaped PolicyException (capabilities/host-namespace/host-path/privileged/run-as-non-root/seccomp) and `contextPropagation.enabled: false` (chart default `true` forces hostNetwork + hostPort on the metrics port).
- **S3 IAM is CR-managed in the seaweed operator, not s3.json**: the local cluster migrated to `S3Identity`/`S3Credentials`/`S3Policy`/`S3PolicyBinding` CRs (cmdshift/platform#125) — the CR path hot-reloads (identity changes land via flux reconcile, no `main-s3` restart; the old startup-only restart rule is obsolete for the CR path). The cloud cluster must adopt the same quartet of CRs (per identity, in the equivalent of `objects-config/`) and can drop its s3.json equivalent when it does. Seeded credentials: the S3Credentials controller owner-refs the credential Secrets first, so the ExternalSecrets must use `target.creationPolicy: Merge`. Mechanics: [manifests/bases/objects/README.md](../bases/objects/README.md).

## valuesFrom pattern (local rollout 2026-09-07, issue cmdshift/platform#31)

All local HelmReleases now ship values via configMapGenerator → `valuesFrom` (per-dir `kustomization.yaml`, per-entry `disableNameSuffixHash`, values in plain `<release>-values.yaml`). When refactoring `manifests/cloud/`, adopt the same layout — canonical why + conventions in `manifests/README.md` (Conventions → valuesFrom everywhere). `helm_verify` already resolves `valuesFrom` refs on both clusters.

## Raw metrics: kps → OTel-only + mimir (LGTM migration cmdshift/platform#128, Prometheus removal cmdshift/platform#141; local 2026-09)

The thanos-operator (bundle.yaml via kustomization, Query/Ruler/Store/Compact CRs, the objstore secret + bucket) is **removed locally**, and kube-prometheus-stack followed it — a hand-rolled monolithic Mimir StatefulSet (`grafana/mimir:3.2.0`, `-target=all`) is the raw store, the OTel collector scrapes and remote-writes into it, and the mimir ruler evaluates the alert rules. The cloud cluster should adopt the mimir shape directly (or an equivalent), keeping these landmines from the local migration:

- **Mimir has no downsampling** (grafana/mimir#1834) — retention is flat raw-only via `-compactor.blocks-retention-period`, which in 3.2.0 is **CLI-only** (no yaml key; the documented `compactor_blocks_retention_period` is rejected by the config struct). Re-decide retention for the cloud, don't carry the local 168h.
- **Mimir config gotchas**: `blocks_storage.s3.bucket_name` (not `bucket`); `compactor.data_dir` must not overlap `blocks_storage.tsdb.dir`; the `runtime_config` file must exist at startup (empty `{}` file); the query API serves under `/prometheus` with a required `X-Scope-OrgID` header (grafana datasource header wiring: `jsonData.httpHeaderName1`/`secureJsonData.httpHeaderValue1`); local-backend ruler scans `<dir>/<tenant>/*.yaml`.
- The thanos-operator-specific notes that used to live here (bundle-vs-chart 1MB cap precedent, `Ready`-condition `healthCheckExprs` gating, GitRepository/quay lockstep) are dead — the operator is gone. The `healthCheckExprs` pattern itself remains the standard for operator-managed CRs.

## datastores group (local 2026-09-08, cmdshift/platform#49)

Local gained a `datastores/` group — cloudnative-pg, valkey-operator, emqx-operator (MQTT broker operator; replaced the NATS + NACK queue stack, cmdshift/platform#64, which had replaced the rabbitmq operators, cmdshift/platform#52). Nothing instantiated yet; no local-only settings inside (all admission-clean installs) — mirror the group when refactoring `manifests/cloud/`, same channels, and reuse the manifests as-is (the emqx install is the CNPG CRD pattern: chart `skipCRDs: true` + the `emqx-operator-crds` child kustomization, whose GitRepository tag must be bumped in lockstep with the chart version). KubeBlocks was tried and rejected there (multi-engine operator, not single-purpose — don't resurrect it in the cloud); full rationale in `manifests/bases/datastores/README.md`.

## Bootstrap module (when added)

Replicate the local bootstrap's readiness gate (cmdshift/platform#72): a `data "http"` poll of the kube API endpoint (CA-pinned) gating the first kubernetes/helm resources, so apply blocks in plan until the API serves instead of relying on a manual wait-and-re-run after node provisioning.

## Image pulls: caching-registry mechanics are local-only (cmdshift/platform#62)

The pull-through caching registry (angos at `registry.cloud.test`, wildcard containerd mirror in the node machine config) is Talos-in-Docker local-only — cloud nodes pull direct from upstreams: no angos companion, no machine-config mirror. The wildcard's strictness (any registry outside the angos upstream map hard-fails at pull, `skipFallback: true`) therefore does not apply in the cloud; mechanics in `runbooks/local/cluster-rebuild.md`.

## Companion haproxy TLS (local: `cluster/local/external/`, cmdshift/platform#130)

The `*.cloud.test` TLS termination (:443, wildcard leaf from the platform intermediate via `just certs`, pem uploaded into the cloud-test container) is **Talos-in-Docker local-only** — there is no companion haproxy in the cloud. The cloud cluster's ingress/auth endpoints follow the cloud-side pattern (real LB / Gateway-API ingress), not this companion haproxy; nothing to port.

## OIDC identity provider (local: `cluster/local/auth/`, cmdshift/platform#131)

The keycloak companion (`auth.cloud.test`) and the kube-apiserver OIDC wiring are **Talos-in-Docker local-only** — the local companion module (`cluster/local/auth/`), the machine-config patches (`cluster.apiServer.extraArgs` OIDC flags, the planted `/var/etc/oidc/ca.crt` + `extraVolumes` in the ctrl patch), and the `.tmp/kubeconfig-oidc` kubelogin kubeconfig all ride the local docker topology. The cloud cluster **resolves its own IdP in-cluster** (a real deployment, not a companion); run the apiserver OIDC flags against that issuer's URLs and CA instead of porting the local patch set. No `# remove in the cloud` manifest marker applies — the companion is terraform/docker, not flux-managed. The RBAC bindings keyed on the OIDC `groups` claim (`platform-admin` → `cluster-admin`, `platform-view` → `view` in `default`) live in the flux-managed `manifests/bases/access/` group and **carry over unchanged** — only the issuer behind the claim differs (resolved in cmdshift/platform#91).

- **kyverno `global.image.registry: ghcr.io`** (override of the chart's reg.kyverno.io default; rationale comment in `manifests/bases/policies/kyverno-values.yaml`): harmless in the cloud — a registry override works the same without the cache, and ghcr is the real upstream behind reg.kyverno.io anyway.
- **vpa + goldilocks** (in the local `observability` group): port as-is, including the lowered VPA recommendation floors (2m CPU / 10Mi) — nothing in the sizing stack depends on the local cluster shape. The VPA CRDs ship in the chart's `crds/` dir (`install.crds: Create` / `upgrade.crds: CreateReplace`), so no separate CRD handling either. Group decisions: `manifests/bases/observability/README.md`.
