apiVersion: v1
kind: ResourceQuota
metadata:
  name: compute
  namespace: storage
spec:
  hard:
    # sized to the namespace audit (cmdshift/platform#93); 2x request-sum headroom
    requests.cpu: 100m
    requests.memory: 200Mi
    limits.cpu: 500m
    limits.memory: 400Mi
    pods: "4"
