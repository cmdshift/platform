apiVersion: policies.kyverno.io/v1
kind: PolicyException
metadata:
  name: allow-velero-security-contexts
  namespace: kyverno
spec:
  policyRefs:
    - name: disallow-capabilities-strict
      kind: ValidatingPolicy
    - name: disallow-host-path
      kind: ValidatingPolicy
    - name: require-run-as-nonroot
      kind: ValidatingPolicy
    - name: restrict-seccomp-strict
      kind: ValidatingPolicy
  matchConditions:
    - name: match-velero
      # velero/node-agent pods by name, plus the temporary data mover pods velero creates
      # for fs-backup/restore — those are named after their PodVolumeBackup/PVR (arbitrary
      # prefix) but always carry the velero.io/pod-volume-backup (or restore) label
      expression: "object.metadata.namespace == 'velero' && (object.metadata.name.startsWith('velero') || object.metadata.name.startsWith('node-agent') || 'velero.io/pod-volume-backup' in object.metadata.labels || 'velero.io/pod-volume-restore' in object.metadata.labels)"
