apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: backups-config
  namespace: flux-system
spec:
  interval: 10m
  retryInterval: 5s
  dependsOn:
    - name: backups
    - name: secrets-config
  sourceRef:
    kind: Bucket
    name: main
  path: ./manifests/local/backups-config
  prune: true
  wait: true
  timeout: 10m
