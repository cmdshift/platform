apiVersion: cilium.io/v2
kind: CiliumNetworkPolicy
metadata:
  name: secrets
  namespace: secrets
spec:
  endpointSelector: {}
  egress:
    # K8s API server
    - toEntities:
        - kube-apiserver
    # Webhook secret store
    - toFQDNs:
        - matchName: secrets.cloud.test
      toPorts:
        - ports:
          - port: "80"
            protocol: TCP
