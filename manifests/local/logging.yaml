apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: logging
  namespace: flux-system
spec:
  # drift-heal only: propagation is event-driven; manual flow = sync_wait + flux_wait
  interval: 1h
  retryInterval: 5s
  dependsOn:
    - name: sources
    - name: secrets-config
    # was: storage — the StorageClasses split out into storage-config (#35);
    # loki-values.yaml names storageClassName: local-path
    - name: storage-config
    - name: monitoring
  sourceRef:
    kind: Bucket
    name: main
  path: ./manifests/local/logging
  prune: true
  wait: true
  timeout: 10m
