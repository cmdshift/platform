apiVersion: external-secrets.io/v1
kind: ClusterSecretStore
metadata:
  name: main
  namespace: secrets
spec:
  provider:
    webhook:
      # :80 required (provider forces HTTPS otherwise); remoteRef.key is URL encoded — janky provider
      url: "http://secrets.cloud.test:80/{{ .remoteRef.key | replace \"%2F\" \"/\" }}"
      result:
        jsonPath: "$"
  conditions:
    - namespaces:
        - flux-system
        - certificates
        - objects
        - observability
        - backups
