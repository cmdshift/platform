apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: certificates-config
  namespace: flux-system
spec:
  interval: 1h
  retryInterval: 5s
  dependsOn:
    - name: certificates
    - name: secrets-config
  sourceRef:
    kind: Bucket
    name: main
  path: ./manifests/local/certificates-config
  prune: true
  wait: true
  timeout: 5m
  healthCheckExprs:
    - apiVersion: cert-manager.io/v1
      kind: ClusterIssuer
      current: status.conditions.filter(e, e.type == 'Ready').all(e, e.status == 'True')
      failed: status.conditions.filter(e, e.type == 'Ready').all(e, e.status == 'False')
    # the ztunnel CA must be issued (secret populated) before networking
    # reconciles — cilium's ztunnel pods mount cilium-ztunnel-secrets
    # non-optionally and fail with FailedMount otherwise (cmdshift/platform#87)
    - apiVersion: cert-manager.io/v1
      kind: Certificate
      current: status.conditions.filter(e, e.type == 'Ready').all(e, e.status == 'True')
      failed: status.conditions.filter(e, e.type == 'Ready').all(e, e.status == 'False')
