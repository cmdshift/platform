crds:
  create: false

image:
  # appVersion default — pinned explicitly for the admission no-floating-tags rule
  tag: "1.30.0"

# audit: 7d peak 6.1m/65Mi — 15m/96Mi req, limits 1.5x
resources:
  requests:
    cpu: 15m
    memory: 96Mi
  limits:
    cpu: 1000m
    memory: 144Mi
