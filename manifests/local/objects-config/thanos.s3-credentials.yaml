apiVersion: seaweed.seaweedfs.com/v1
kind: S3Credentials
metadata:
  name: thanos
  namespace: objects
spec:
  seaweedRef:
    name: main
    namespace: objects
  identityRef:
    name: thanos
  secretRef:
    name: thanos-s3-credentials
