apiVersion: seaweed.seaweedfs.com/v1
kind: S3Policy
metadata:
  name: thanos
  namespace: objects
spec:
  seaweedRef:
    name: main
    namespace: objects
  statements:
    - effect: Allow
      actions:
        - s3:ListBucket
      resources:
        - thanos
    - effect: Allow
      actions:
        - s3:GetObject
        - s3:PutObject
        - s3:DeleteObject
      resources:
        - thanos/*
