# Kubescape SecurityException — see kyverno.security-exception.yaml header.
apiVersion: kubescape.io/v1beta1
kind: SecurityException
metadata:
  name: operators-are-api-clients
  namespace: seaweedfs-operator
spec:
  reason: the operator watches/manages Seaweed CRs via the API
  author: platform
  posture:
    - controlID: C-0034
      action: ignore
  match:
    resources:
      - kind: Deployment
        name: seaweedfs-operator
