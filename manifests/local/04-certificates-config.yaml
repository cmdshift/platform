apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: certificates-config
  namespace: flux-system
spec:
  interval: 1m
  retryInterval: 5s
  dependsOn:
    - name: secrets-config
    - name: certificates
  sourceRef:
    kind: Bucket
    name: main
  path: ./manifests/local/04-certificates-config
  prune: true
  wait: true
  timeout: 5m
