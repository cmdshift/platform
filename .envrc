#!/bin/sh

export STEPPATH="$PWD/cluster/local/.tmp/tls"
export KUBECONFIG="$PWD/cluster/local/.tmp/kubeconfig"
export TALOSCONFIG="$PWD/cluster/local/.tmp/talosconfig"
export DOPPLER_PROJECT=platform
export DOPPLER_CONFIG=local_personal

PATH_add tools/bin
